# ShadowSensor — Live Status

*Single shared status file. Paste the current version of this file into the start of any Codex or dedicated Claude session prompt so that surface knows exactly where the project stands, with no need to recall prior conversations. Maintained by the ShadowSensor Prompt Agent — updated every time Ayush brings a progress report back.*

**Last Updated:** 2026-08-13 — **Phase 7B COMPLETE ✅.** All three subphases done. RF classifier trained (ROC-AUC 0.837), integrated into live scorer, dashboard wired, `docs/phase7b_report.md` written. 25 new tests total (SP1: 12, SP2: 6, SP3: 7). Full suite: 705 passed, 0 failed. Commits pending. Phase 8A (Alert Correlation) is next.

---

## Current Phase
**Phase 8A — Alert Correlation / Severity Engine — NOT STARTED**

Next up: alert deduplication, severity tiering, `suspected_families` field population. See `ShadowSensor_Master_Implementation_Plan.md` Phase 8A section.

**Phase 7B — Train Random Forest — COMPLETE ✅**

- Subphase 1 (RF Training Script): **COMPLETE ✅** — 2026-08-13. `ml/training/train_random_forest.py` created. Model: `ml/models/random_forest.joblib`. Metrics JSON: `docs/phase7b_metrics.json`. 12 new tests (692 total). ROC-AUC 0.837 ± 0.014. Top features: parent_cmd_length, cmd_entropy, unique_rules_fired.
- Subphase 2 (RF Scorer Integration): **COMPLETE ✅** — 2026-08-13. `ml/scoring/scorer.py` modified — RF artifact loaded at startup, RF `model_scores` row written per event (non-fatal). 6 new tests (698 total). RF row confirmed: model_type='random_forest', score=0.6.
- Subphase 3 (Dashboard Wire-Up + Report): **COMPLETE ✅** — 2026-08-13. `get_random_forest_status()` added to service. Router wired. Template RF placeholder replaced with live data section. `docs/phase7b_report.md` written. 7 new tests (705 total). Dashboard: HTTP 200, RF badge "Awaiting Data", IF section unaffected.

**Phase 7B — Train Random Forest — COMPLETE ✅**

**Phase 7A — Generate Labeled Suspicious Telemetry via Sandbox Simulation — COMPLETE ✅**

- Subphase 1 (PowerShell, 11 rules): **COMPLETE** ✅ — 2026-08-12 re-simulated (§21.5 reversal) — 312 rows exported (`suspicious_ps.csv`), 6 PASS / 5 PARTIAL (2 D-f blocked, 3 pipeline-lag — hits confirmed in DB), 0 SKIPPED. Prior partial CSVs superseded.
- Subphase 2 (LOLBins, 13 rules): **COMPLETE** ✅ — 2026-08-12 re-simulated (§21.5 reversal) — 9 PASS / 2 PARTIAL (D-f: RUNDLL32, REGSVR32) / 1 PARTIAL-lag (CERTUTIL Path A) / 1 FAIL (HH_CHM, all paths 0 hits — new env finding D45, likely Sysmon config exclusion or pre-EID-1 Defender kill). RUNDLL32 Path C unexpectedly PASS (http:// form not Defender-blocked). Feature CSV: `suspicious_lolbins.csv`, 196 rows, 31 features, label=1.
- Subphase 3 (Network, **9 rules** — live YAML audit 2026-08-12): **COMPLETE** ✅ — 2026-08-12 re-simulated (§21.5 reversal). Script: `scripts/simulate_subphase_3.py`. Feature CSV: `data/features/suspicious_network.csv` (194 rows, 31 features, label=1). SIM window: 15:04–15:56 UTC. Tally: 2 paths PASS from NET_POWERSHELL_HTTP_001, 2 from NET_DNS_LONG_QUERY_001, 1 from NET_SCRIPTING_ENGINE_HTTP_001, 1 from NET_LOLBIN_NETWORK_001 (odbcconf) — 4 new env findings D46–D49. 2 SKIP (D28). INCONCLUSIVE cluster confirmed (D29). FP suppression FAIL (D49 — EID-3 destination_hostname always NULL). 16/194 rows carry has_network_rule_hit=1.
- Subphase 4 (Parent-Child Chains, **10 live rules** — live YAML audit 2026-08-12): **COMPLETE** ✅ — 2026-08-12 re-simulated (§21.5 reversal). Script: `scripts/simulate_subphase_4.py`. Feature CSV: `data/features/suspicious_chains.csv` (186 rows, 31 features, label=1). DB window: 16:35:25–16:55:49 UTC. Tally: 3 PASS (SCRIPT_HOST_CMD, SCRIPT_HOST_POWERSHELL, SCHEDULED_TASK_SVCHOST) / 1 PARTIAL (LOLBIN_CHILD — mshta+regasm PASS, cmstp FAIL D51) / 1 PARTIAL (BROWSER_SHELL D42) / 2 FAIL (SCHEDULED_TASK_SCRIPT D43; REGSVR32_CHILD D50 — new finding) / 3 SKIP (Office). D41 not triggered — wscript/cscript spawned cleanly. No D-f blocks on any path.
- Subphase 5 (API/OpenProcess, **8 rules** — `api_memory.yaml`): **COMPLETE** ✅ — 2026-08-13, SIM window 02:53–03:44 UTC. Script: `scripts/simulate_subphase_5.py`. Feature CSV: `data/features/suspicious_api.csv` (pending DB query + feature extraction — see NEXT STEPS). Tally: EID-8 (CRT) both PASS (Rules 7/8 ✓, E5 workaround confirmed, CRT to lsass/winlogon confirmed). EID-10 rules 1/2/4 PARTIAL — 2/3 paths PASS each (D52: denied on csrss/services PPL — Sysmon doesn't log denied access). Rule 3 (AV_PROCESS) FAIL/D53 — Sysmon ProcessAccess filter excludes fake msmpeng; real MsMpEng is PPL. EID-7 rules 5/6 FAIL/D54/D55 — Sysmon ImageLoad filter does not capture python/rundll32/regsvr32 DLL loads on this VM. Defender also quarantined `C:\Users\Public\ss_api.dll` mid-simulation. D30 pre-flight: clean window (no background hits). 4 new D-findings: D52–D55.
- Subphase 6 (CreateRemoteThread/Injection): **SUPERSEDED** — §21.5 re-simulation consolidated all 8 `api_memory.yaml` rules (EID-10 + EID-7 + EID-8) into `simulate_subphase_5.py`. EID-8 rules (`API_CRT_SUSPICIOUS_SOURCE_001`, `API_CRT_SENSITIVE_TARGET_001`) both PASS, confirmed in `suspicious_api.csv`. No separate SP6 script or CSV needed.
- Fix session (post-Subphase-5, pre-Subphase-7): **COMPLETE** — E1 CLOSED, E2 CLOSED. 669 → 680 tests. See handover.md Sections 22 and 24.
- Subphase 7 (Consolidation & Handoff): **COMPLETE** ✅ — 2026-08-13. Combined `suspicious.csv`: 1,105 rows (312+196+194+186+217), 31 cols (30 features + label), all label=1, zero contamination. IF score: mean 0.2152 (vs benign 0.1394), 14.7% anomalous (vs benign 5.0%). `docs/phase7a_final_report.md` written. 15 rules with no label=1 coverage (Office absent, D28/D45/D52-D55 VM limits) — documented in report §8. Phase 7A fully closed.

**Phase 6B — Isolation Forest Training & Real-Time Scoring Integration: COMPLETE ✅**
**Phase 6A — Benign Baseline Collection: COMPLETE ✅**
**Phase 5 — Feature Engineering Pipeline: COMPLETE ✅**

---

## Full Phase Tracker

*Condensed view — full detail lives in `ShadowSensor_Master_Implementation_Plan__2_.md`. Timeline currently tracking ~52 days (~7.4 weeks) after AV-style dashboard & tray icon feature additions.*

| Phase | Sub-step | Surface | Status |
|---|---|---|---|
| 0 | 0A — Build & validate Validation Lab | Dedicated Claude session | **Complete** ✅ |
| 0 | 0B — Repo + deps + standards | Codex | **Complete** ✅ |
| 1 | Event Collector & Normalizer | Codex | **Complete** ✅ |
| 2 | 2A — Rule engine core + starter rules | Codex | **Complete** ✅ |
| 2 | 2B — Validate starter rules in sandbox | Cursor (Claude Sonnet 4.6) | **Complete** ✅ |
| 3 | Storage + Dashboard MVP | Codex 5.3 / Auto-selection | **Complete** ✅ |
| Hold | Kill Chain Vis — Subtask A | Codex / Cursor | Complete |
| Hold | Kill Chain Vis — Subtask B | Codex / Cursor | Complete |
| Hold | Kill Chain Vis — Subtask C | Codex / Cursor | Complete |
| 4 | 4A — Full rule expansion | Codex | **Complete** ✅ |
| 4 | 4B — Validate all rules in sandbox *(+ family-template enrichment)* | Dedicated Claude session | **Complete** ✅ |
| 5 | Feature Engineering pipeline | Codex | **Complete** ✅ |
| 6 | 6A — Benign baseline collection | Dedicated Claude session | **Complete** ✅ |
| 6 | 6B — Train Isolation Forest | Cursor (Claude Sonnet 4.6) | **Complete** ✅ |
| 7 | 7A — Generate labeled suspicious telemetry *(+ family-template enrichment)* | Dedicated Claude session | **Complete** ✅ |
| 7 | 7B — Train Random Forest | Cursor Grok 4.6 High Fast | **Complete** ✅ |
| 8 | 8A — Alert correlation/severity engine *(+ `suspected_families` field)* | Codex | Not Started |
| 8 | 8B — Validate severity tiers | Dedicated Claude session | Not Started |
| 9 | 9A — Packaging + service wrapper *(+ tray companion app — 1 → 3 days)* | Codex | Not Started |
| 9 | 9B — Fresh-install validation (Win10/11) *(+ tray validation — 1 → 1.5 days)* | Dedicated Claude session | Not Started |
| 10 | 10A — Run evaluation, generate metrics | Codex | Not Started |
| 10 | 10B — Research synthesis & paper draft *(+ Section 3.5 technique-to-family table)* | Dedicated Claude session | Not Started |

---

## Recent Activity (most recent first)

- 2026-08-13 — **Phase 7B Subphase 3 (Dashboard Wire-Up + Report) COMPLETE — Phase 7B FULLY CLOSED ✅.** `dashboard/services/ml_insights_service.py`: added `get_random_forest_status()` (mirrors IF function, queries `model_type='random_forest'`). `dashboard/routers/pages.py`: router now calls `get_random_forest_status()` and passes live RF dict to template. `dashboard/templates/ml_insights.html`: RF placeholder block (Phase 7B / `ml-status-future`) fully replaced with live data section — shows "Awaiting Data" badge when no RF rows, "Active" with stats/distribution when RF rows present. `docs/phase7b_report.md` written (training data, feature set, CV results, feature importance, Phase 7A coverage gaps, research paper notes). 7 new tests. Dashboard: HTTP 200, RF badge "Awaiting Data", IF section unaffected. Full suite: 705 passed, 0 failed (independently verified). Phase 7B complete — 25 new tests total (SP1: 12, SP2: 6, SP3: 7). Phase 8A next.

- 2026-08-13 — **Phase 7B Subphase 2 (RF Scorer Integration) COMPLETE.** `ml/scoring/scorer.py` modified: added `import joblib`, `import pandas as pd`, `RF_MODEL_PATH` import, `_rf_artifact` load block in `__init__`, and Step 5 in `score_and_persist()` writing `ModelScoreRecord(model_type='random_forest', ...)`. RF failure is non-fatal — IF score still returned. RF row confirmed: model_type='random_forest', score=0.6. 6 new tests in `tests/test_phase7b/test_rf_scorer.py`. Committee fix: `tests/test_phase6b/test_scoring_integration.py::test_score_and_persist_writes_model_scores_row` updated — was asserting `len(rows)==1`, now asserts `len(rows)>=1` and filters for IF row by model_type (SP2 implementation was correct; test was written before RF existed). 698 total passing, 0 failed. SP3 authorized.

- 2026-08-13 — **Phase 7B Subphase 1 (RF Training Script) COMPLETE.** Script: `ml/training/train_random_forest.py`. Model artifact: `ml/models/random_forest.joblib` (keys: model, feature_names, cv_metrics). Metrics JSON: `docs/phase7b_metrics.json`. Training data: 621 benign + 1,105 suspicious = 1,726 total. Feature set: 28 features (`open_process_suspicious_access` and `hour_of_day` excluded). 5-fold stratified CV: precision 0.818 ± 0.008, recall 0.848 ± 0.028, F1 0.833 ± 0.016, ROC-AUC 0.837 ± 0.014. Top permutation features: parent_cmd_length (0.0197), cmd_entropy (0.0182), unique_rules_fired (0.0112). 12 new tests, 692 total passing. **Research paper note:** behavioral/entropy features dominate over pure rule-hit features in permutation importance — RF generalizes beyond rule coverage. SP2 (scorer integration) authorized.

- 2026-08-13 — **Phase 7A Subphase 5 (API/Memory, 8 rules) COMPLETE.** SIM window: 02:53–03:44 UTC. Script: `simulate_subphase_5.py`. Feature CSV: `suspicious_api.csv` pending DB query. Results: EID-8 (Rules 7/8) **PASS** — CRT to notepad/lsass/winlogon confirmed, E5 workaround (ntdll.RtlExitUserThread) confirmed. EID-10 (Rules 1/2/4) **PARTIAL** — 2/3 paths PASS each (lsass/winlogon/notepad targets fire), Path C FAIL on all three (D52: Sysmon doesn't log denied OpenProcess on csrss/services PPL). Rule 3 AV_PROCESS **FAIL/D53** — handles acquired on fake msmpeng but Sysmon ProcessAccess filter doesn't include fake processes in Temp; real MsMpEng PPL. EID-7 (Rules 5/6) **FAIL/D54/D55** — Sysmon ImageLoad filter on this VM does not capture python.exe or LOLBin DLL loads; rundll32 hung 20s (DLL WAS loaded but no EID-7); Defender quarantined `C:\Users\Public\ss_api.dll` mid-simulation. D30 pre-flight: clean. 4 new D-findings: D52–D55.

- 2026-08-12 — **Phase 7A Subphase 4 (Parent-Child Chains, 10 rules) re-simulation COMPLETE.** SIM window: 2026-08-12 16:35–16:56 UTC. Feature extraction pending. Path-level: CHAIN_SCRIPT_HOST_CMD_001 (A/B/C: PASS), CHAIN_SCRIPT_HOST_POWERSHELL_001 (A/B/C: PASS — SysWOW64 path confirmed), CHAIN_SCHEDULED_TASK_SVCHOST_001 (A/B/C: PASS — all 3 command-line variants confirmed), CHAIN_SCHEDULED_TASK_SCRIPT_001 (A/B/C: FAIL — D43 experimentally confirmed), CHAIN_REGSVR32_CHILD_001 (A/B/C: FAIL — new D50: regsvr32 + .NET COM DLL spawns child but Defender silently terminates before Sysmon EID-1; regasm with same DLL = PASS, confirming C# code correct), CHAIN_LOLBIN_CHILD_001 (A: PASS mshta HTA, B: FAIL D51 cmstp INF, C: PASS regasm), CHAIN_BROWSER_SHELL_001 (PARTIAL D42), CHAIN_OFFICE_* (SKIP). D41 not triggered — clean run, no retries needed. DLL compilation: PASS. Scheduled task cleanup: all 4 tasks deleted.

- 2026-08-12 — **Phase 7A Subphase 3 (Network, 9 rules) re-simulation COMPLETE.** SIM window: 2026-08-12 15:04–15:56 UTC. Feature CSV: `suspicious_network.csv`, 194 rows, 31 features, label=1. 16/194 rows with `has_network_rule_hit=1`. Path-level results: NET_POWERSHELL_HTTP_001 (A: PASS, B: FAIL-D48, C: PASS), NET_DNS_LONG_QUERY_001 (A: PASS, B: PASS, C: FAIL-D47), NET_DNS_SCRIPT_ENGINE_001 (A/B: INCONCLUSIVE-D29, C: PARTIAL — mshta 1 hit), NET_SCRIPTING_ENGINE_HTTP_001 (A: PASS — wscript https works!, B/C: INCONCLUSIVE-D29), NET_SCRIPT_ENGINE_OUTBOUND_001 (A/B: INCONCLUSIVE-D29, C: FAIL-D46), NET_LOLBIN_PROCESS_HTTP_001 (A: FAIL-D46, B/C: INCONCLUSIVE-D-b), NET_LOLBIN_NETWORK_001 (A: FAIL-D46, B: INCONCLUSIVE-D-b, C: PASS — odbcconf), NET_SUSPICIOUS_PORT_001/NET_SMB_LATERAL_001: SKIP (D28). FP suppression FAIL: D49 (EID-3 destination_hostname = NULL → hostname exclusions ineffective). New findings: D46 (mshta EID-3 invisible for HTTP/HTTPS but can generate EID-22 DNS), D47 (nslookup bypasses Windows DNS resolver → 0 EID-22), D48 (port-80 connections from PS/wscript/cscript to 8.8.8.8 → 0 EID-3; odbcconf same target → 4 hits — process-dependent EID-3 behaviour), D49 (EID-3 destination_hostname always NULL → hostname-based rule exclusions are structurally ineffective on this VM). Subphase 4 prompt ready (`prompt_subphase4.md`).

- 2026-08-12 — **Phase 7A Subphase 2 (LOLBins, 13 rules) re-simulation COMPLETE.** 9 PASS, 2 PARTIAL (D-f: RUNDLL32_SUSPICIOUS, REGSVR32 — pre-execution Defender kill), 1 PARTIAL-lag (CERTUTIL Path A — -urlcache blocked, Paths B/C PASS), 1 FAIL (HH_CHM — all 3 paths 0 hits after 180s poll each — flagged as D45: Sysmon or Defender prevents EID-1 for hh.exe with URL/javascript args on this VM). Unexpected PASS: RUNDLL32 Path C (http:// form) fired — not in D-f block list. .NET tools found at Framework64\v4.0.30319 (REGASM, REGSVCS, INSTALLUTIL all PASS). Staging: `exports/subphase_2_training.csv`. Feature extraction: `suspicious_lolbins.csv`, UTC 09:35–11:30. Prompt for Subphase 3 next.

- 2026-08-12 — **Phase 7A Subphase 1 re-simulated COMPLETE** (§21.5 CSV reversal — fresh multi-path run). All 11 `powershell.yaml` rules covered. New `simulate_subphase_1.py` ran to completion with 3-path / 2-launch per rule structure. 6 PASS, 5 PARTIAL (2 Defender-blocked per D-f: PS_AMSI_BYPASS_001, PS_CREDENTIAL_ACCESS_001; 3 pipeline-lag: PS_HIDDEN_WINDOW_001 Paths B/C, PS_EXECUTION_POLICY_BYPASS_001 Paths A/B, PS_INVOKE_EXPRESSION_001 Path B — all confirmed in DB via diag). Key new finding logged as D44: pipeline lag on this VM reaches 10–15 minutes for some events — stale hits from prior script runs bleed into subsequent FP test windows via `quick=True` query; FP hard stops converted to WARN after confirming svchost.exe-parented PS was correctly suppressed by rule engine. Staging CSV: `exports/subphase_1_training.csv`. Feature extraction: `suspicious_ps.csv`, 312 rows, 31 features, label=1, UTC window 07:57–09:00. Replaces prior `suspicious_ps_partial1.csv` / `suspicious_ps_partial2.csv` (152 rows). Phase 7A Subphase 2 prompt drafted (`prompt_subphase2.md`), ready for Grok.

- 2026-08-11 — **Issue Catalog E2 CLOSED.** Full detail in
  handover.md Section 24. call_trace not_contains_any condition added
  to API_OPEN_PROCESS_VM_WRITE_001 with allow_null: true and three
  suppression strings (NGen and non-NGen PowerShell automation
  assembly variants). Engine: None guard added to _op_not_contains_any.
  Mid-session discovery: _evaluate_condition short-circuits on None via
  allow_null before reaching the operator — requiring allow_null: true
  on the YAML condition and a Test 7 (None via full engine path) beyond
  the original six. powershell.exe untouched as source throughout.
  Test suite: 669 → 680 passed, 0 failed, 51 rules unchanged.
- 2026-08-11 — **Issue Catalog E1 CLOSED.** Full detail above and in
  docs/decisions_log.md Entry 014 / handover.md Section 22. Test suite
  662 -> 669 (+7: +3 Subphase 1, +4 Subphase 2, +0 Subphase 3). Rule
  count unchanged at 51. All changes remain uncommitted working-tree
  diffs on top of cda95c3, per the still-binding deferred-commit
  decision. E2 next.
- 2026-08-10 — **Phase 7A Subphase 5 & 6 COMPLETE** (post-C4 re-verification + CRT validation). Subphase 5: pipeline confirmed 51 rules, 8 total in `api_memory.yaml` audited live (not assumed from docs). 3 rules needed re-test after C4/Category A-D fixes — all 3 confirmed against real telemetry via Sim1c/5c/6c: `API_TOKEN_MANIPULATION_001` (D47 hex-padding fix, `0x40` unpadded matched correctly), `API_OPEN_PROCESS_SUSPICIOUS_ACCESS_001` (D49+C3, over-grant `0x1f3fff` matched), `API_OPEN_PROCESS_VM_WRITE_001` (D49+C4, over-grant `0x1038` matched). Clean idle pipeline window also confirmed C4's original 5-hit false-positive pattern does not recur. 2 rules already PASS (no re-run needed), 1 environmentally limited (PPL/D45, unchanged). Subphase 6: both `API_CRT_SUSPICIOUS_SOURCE_001` and `API_CRT_SENSITIVE_TARGET_001` confirmed firing correctly and independently (lsass target fires both simultaneously as designed; notepad target fires only SUSPICIOUS_SOURCE, confirming no over-firing). Original Atomic Red Team template abandoned after a 4-issue causal chain (E3: template can't reach SUSPICIOUS_SOURCE_001's source allowlist; E4: Atomic Red Team binary blocked by Defender; E5: Sysmon config excludes CreateRemoteThread events with StartModule=kernel32.dll — genuine detection blind spot, deferred fix bundled with D28; E6: workaround script authoring slip, corrected) — replaced with a custom P/Invoke script using RtlExitUserThread from ntdll.dll. No rule defects found in either CRT rule at any point in this chain. Full depth in new file `phase7a_subphase5-7_issues_log.md` (E1-E6) alongside 2 additional VM_WRITE noise findings (E1: compiler-toolchain cascade via Add-Type/csc.exe/cvtres.exe; E2: PowerShell process-management cmdlets incidentally acquiring broad handles) both deferred to a future consolidated fix session. **New standing workflow: no CSV export until Subphase 7 fully simulated, single consolidated fix session after, re-simulate fresh, then export.** Open question flagged, not decided: Subphases 1-4's already-exported CSVs may be stale relative to Category A/B fixes touching CHAIN_SCHEDULED_TASK_SCRIPT_001 and NET_SCRIPT_ENGINE_OUTBOUND_001 (see handover.md Section 21.5). No code/rule/test changes made this session — 662 passed, 0 failed, 51 rules, unchanged since C4. Hard stop — awaiting go-ahead for Subphase 7 (scope needs clarifying given the new no-export workflow — see handover.md Section 21.9).
- 2026-08-07 — **Issue Catalog C4 CLOSED** (standalone, post-Category-C). `API_OPEN_PROCESS_VM_WRITE_001` false positives discovered at Phase 7A Subphase 5 re-verification pipeline startup — 5 Critical-severity hits in under a minute, zero simulation running (self-updating software, Sysmon's own instrumentation, and routine SCM service-start activity). Root cause: pre-existing rule-design gap, not an engine regression — `bits_any_set` (D49) confirmed evaluating correctly throughout. Fixed across 3 subphases in a single `task.md`, each in its own fresh Cursor chat: Subphase 1 added a `not_same_basename` source-vs-target self-reference check (reused an existing, previously-unused engine operator — zero engine changes); Subphase 2 path-anchored the `source_image` exclusion list (C3-style) and added 3 new exclusions (`RunOnce.exe`, `Sysmon64.exe`, `services.exe`); Subphase 3 raised the `granted_access` permission floor to require the `PROCESS_VM_WRITE | PROCESS_VM_OPERATION` pairing `WriteProcessMemory` actually needs, removing the redundant bare-`PROCESS_VM_WRITE` floor that had made 4 of 5 configured masks dead weight under `bits_any_set` OR semantics. Committee explicitly considered and rejected adding a `target_image` restriction (would mirror the sibling rule's sensitive-target allowlist but create an exploitable blind spot, since real injection deliberately targets non-sensitive processes to evade exactly that kind of check). Full detail: `docs/decisions_log.md` Entry 013. Test suite: 655 → 662 passing, 0 failed (+7 net, 1 existing test deliberately modified in place). Rule count unchanged at 51. All changes remain uncommitted working-tree diffs on top of commit `cda95c3`. **Phase 7A Subphase 5's actual re-simulation (Sim1c/5c/6c-equivalents, per handover.md Section 19.2) has NOT yet run** — hard stop, awaiting go-ahead.
- 2026-07-30 — **Phase 7A Subphase 4 (Parent-Child Chains) COMPLETE.** Live YAML audit of `parent_child.yaml` found 10 live rules (not task.md's estimated 9-10) — full mapping logged as D31–D39 (renames, scope expansions, undocumented rules, one rule — `CHAIN_OFFICE_LOLBIN_001` — with no live equivalent at all). 3 rules SKIPPED (Office not installed: `CHAIN_OFFICE_POWERSHELL_001`, `CHAIN_OFFICE_CMD_001`, `CHAIN_OFFICE_WSCRIPT_001`). Of 7 runnable rules: 5 PASS (`CHAIN_SCRIPT_HOST_CMD_001` — required one retry, see D41; `CHAIN_SCRIPT_HOST_POWERSHELL_001`; `CHAIN_REGSVR32_CHILD_001`; `CHAIN_SCHEDULED_TASK_SVCHOST_001`; `CHAIN_LOLBIN_CHILD_001`), 1 PARTIAL (`CHAIN_BROWSER_SHELL_001` — command-line automation can't route Edge's protocol handler correctly, see D42; rule disposition remains PASS standing on Phase 4B's prior direct validation, no fresh telemetry generated this subphase — deliberate tradeoff, not an oversight), 1 FAIL (`CHAIN_SCHEDULED_TASK_SCRIPT_001` — structural, see D43, flagged for Codex). Major findings this subphase: D40 (WScript.Shell as a COM object inside PowerShell attributes the child process to powershell.exe, not wscript.exe — cannot substitute for genuine wscript.exe-hosted simulations), D41 (wscript.exe→shell-interpreter spawning is intermittently, non-deterministically silent-failing on this VM with no Defender detection logged — retry once before concluding FAIL), D42 (Edge protocol-handler simulation requires an actual in-browser trigger, not command-line automation), D43 (`CHAIN_SCHEDULED_TASK_SCRIPT_001`'s YAML now lists schtasks.exe as a valid parent but it structurally never is one in practice — confirmed real parent is always `svchost.exe -s Schedule` — recommend Codex deprecate or explicitly document as legacy-only). D30 (Issue 4 pattern) recurred a third time at pre-flight, still idle/unprompted only — flagged strongly for Subphase 5. Export: DB-confirmed UTC window 06:04:56–06:16:42, 129 process windows, verified uncontaminated (100% label=1), `has_chain_rule_hit` active on exactly 6/6 rows matching confirmed triggers (including one rule firing twice from separate process windows). Hard stop — awaiting go-ahead for Subphase 5 (API/OpenProcess).
- 2026-07-30 — **Phase 7A Subphases 2 and 3 confirmed CLOSED** (status.md catch-up — both were completed and verified in-session previously but not yet reflected here, per the recurring status.md-drift concern). **Subphase 2 (LOLBins, 13 rules):** live YAML audit found 13 live rules matching 12 documented + 1 undocumented (`LOLBIN_HH_CHM_001`, family: Lazarus Group), plus 1 documented rule (`LOLBIN_FINDSTR_001`) absent from live YAML — SKIPPED. All 13 live rules PASS, plus 1 bonus incidental parent-child rule (`CHAIN_LOLBIN_CHILD_001`, initially logged under its doc name `CHAIN_LOLBIN_SHELL_001` — D15) confirmed as a side effect. Renames logged as D9–D14. Exported `suspicious_lolbins.csv` — 63 rows, verified clean, 14+1 rule-hit-attributable rows matching exactly. **Subphase 3 (Network, 8 rules):** live YAML audit found significant drift (D16–D21) — 3 documented rules absent/superseded, 2 undocumented live rules found (`NET_POWERSHELL_HTTP_001`, `NET_LOLBIN_PROCESS_HTTP_001`/`NET_LOLBIN_NETWORK_001`). Major structural finding **D28** (HIGH PRIORITY): Sysmon's NetworkConnect (EID-3) config only captures ports 80/443 — confirmed via direct config file read — making `NET_SUSPICIOUS_PORT_001` and `NET_SMB_LATERAL_001` structurally unfireable regardless of rule quality; flagged as a dedicated Codex/config task. **D29**: a `cscript.exe`-based HTTPS round-trip to `dns.google:443` completed successfully (HTTP 200 confirmed) yet produced zero Sysmon events of any type — root cause not fully isolated; `NET_DNS_SCRIPT_ENGINE_001` disposition INCONCLUSIVE, flagged as a genuine telemetry visibility gap for the research paper. Also confirmed: Sysmon's EID-3 loopback (127.0.0.1) traffic is not reliably observed — use 8.8.8.8/8.8.4.4 for all network-layer simulations going forward (D22). Final tally: 4 PASS, 2 SKIPPED (D28), 1 INCONCLUSIVE (D29). Exported `suspicious_network.csv` — DB-confirmed UTC window 2026-07-30 04:53:20–05:06:15, 92 process windows, verified clean, `has_network_rule_hit` active on exactly 3 rows matching the 3 distinct underlying simulated processes.
- 2026-07-29 — **Phase 7A Subphase 1 (PowerShell, 11 rules) COMPLETE.** Pre-flight hit and resolved a stale-bookmark pipeline stall (collector had not consumed events since 2026-07-28 14:09 despite Sysmon logging live — bookmark clear + pipeline restart resolved it; VMware shared-folder instability, consistent with prior documented occurrences). Live ruleset audit of `rules/definitions/powershell.yaml` revealed significant drift from documentation: 2 rules documented in task.md/master plan do not exist in the live YAML (`PS_NOPROFILE_NONINTERACTIVE_001`, `PS_OBFUSCATION_001` — SKIPPED), 2 rules exist under different IDs than documented (`PS_REFLECTION_ASSEMBLY_001`→`PS_REFLECTIVE_ASSEMBLY_001`; `PS_CONSTRAINED_BYPASS_001`→`PS_CONSTRAINED_LANG_BYPASS_001`, the latter also using a completely different detection mechanism — `__PSLockdownPolicy` env var, not `-Version 2`), and 2 rules exist in the live YAML with zero documentation anywhere (`PS_VERSION_DOWNGRADE_001`, `PS_WMI_EXEC_001`). Full detail logged as Decisions Log Entry 008 (D1–D8). Final tally: 9 PASS, 2 PARTIAL (`PS_AMSI_BYPASS_001`, `PS_CREDENTIAL_ACCESS_001` — both blocked by Windows Defender pre-execution, confirmed via `Get-MpThreatDetection`, before Sysmon could log a ProcessCreate event; not rule defects), 2 SKIPPED (documented-only rules, do not exist). Flagged and excluded from labeled export: 3× unplanned `API_CREATE_REMOTE_THREAD_001` hits (target=`<unknown process>`) coinciding with Defender's remediation of the AMSI-bypass process — logged as a non-simulated artifact, not genuine injection telemetry, carried forward for awareness at Subphase 6. Discovered and corrected a local-time-vs-UTC mismatch between console `RULE_HIT` output and stored DB timestamps (DB stores UTC; VM is IST/UTC+5:30) — `--since`/`--until` export bounds must use DB-confirmed UTC timestamps, not console wall-clock times, for all future subphases (D8). Final exports: 79 + 73 = 152 labeled (label=1) process windows across `suspicious_ps_partial1.csv` / `suspicious_ps_partial2.csv`, verified uncontaminated (100% label=1) with exactly 8 rule-hit-attributable rows matching the 8 successfully simulated rule triggers (3 in partial1, 5 in partial2).
- 2026-07-28 — **Phase 6B FULLY CLOSED.** Both closure gates resolved: (1) Live VM end-to-end verification CONFIRMED by Ayush — full chain Sysmon → Collector → Normalizer → Rule Engine → Storage → Scoring → `model_scores` → ML Insights dashboard, all 8 other dashboard pages confirmed with no regressions. (2) Git commit `dee6ef3da11fa80744d7150b381bed0ab10514e4` pushed to `origin/main` and verified — 58 files changed, 7985 insertions, 90 deletions (combined Phase 6A + 6B). Final regression: **586 passed, 0 failed**. Stray files `'2026-07-27`, `check_range.py`, `check_range2.py` deleted. `Git_Upload_Commands_Log.md` left untracked/local. Phase 7A/7B ready to begin on Ayush's explicit go-ahead.
- 2026-07-28 — **Phase 6B Subphase 4 complete.** ML Insights dashboard (`/dashboard/ml-insights`) wired to real `model_scores` data. New `dashboard/services/ml_insights_service.py` service layer provides: `get_isolation_forest_status()` (all-time count, min/max/mean/median, 10-bracket distribution) and `get_score_trend()` (hourly averages, last 24h). Template fully redesigned with stats row, CSS distribution bars (color-coded: normal/elevated/anomalous), and ApexCharts trend line chart. `/api/v1/ml-status` wired to return live `models_trained=True` when data present. Random Forest section remains placeholder ("Phase 7B"). 24 new tests added. Full regression: **586 passed, 0 failed**. Hard stop — awaiting Ayush's go-ahead for Subphase 5.
- 2026-07-17 — **Phase 5 complete — Feature Engineering Pipeline fully implemented and end-to-end verified on host PC. 502 tests passing.**

*(older entries trimmed — see prior status.md revisions for full Phase 0–5 history)*

---

## Decisions & Notes Log

- **2026-07-30 — Phase 7A Subphase 4 findings (D30 3rd occurrence, D31–D43).** Full detail in `docs/decisions_log.md` Entries 009–010 (pending file delivery — see note below). Summary: full `parent_child.yaml` audit (10 live rules) resolved several doc-vs-live mismatches (D31–D39, mostly renames/scope-expansions/consolidations, one rule with no live equivalent at all — `CHAIN_OFFICE_LOLBIN_001`). Two real simulation-technique limitations discovered and documented as reusable working knowledge: WScript.Shell invoked as a COM object from inside PowerShell mislabels the parent process (D40); wscript.exe spawning a shell interpreter is intermittently, non-deterministically silent on this VM with no Defender signature — retry once before concluding FAIL (D41). One structural rule-design gap flagged for Codex: `CHAIN_SCHEDULED_TASK_SCRIPT_001` cannot fire in practice on this Windows build despite YAML changes intending to fix it — real parent is always `svchost.exe -s Schedule`, never `schtasks.exe` directly (D43). D30 (Issue 4 pattern) now at 3 unprompted occurrences across Subphases 3–4 pre-flight; a 4th occurrence during deliberate Subphase 5 simulation will trigger resolving its disposition.
- **2026-07-30 — Phase 7A Subphases 2–3 findings (D9–D29), status.md catch-up.** Full detail in `docs/decisions_log.md` Entries 009 (pending file delivery — see note below). Subphase 2 (LOLBins): 6 doc-vs-live renames (D9–D14), 1 rule absent from live YAML (D13, SKIPPED), 13/13 live rules PASS. Subphase 3 (Network): 6 doc-vs-live discrepancies (D16–D21), plus two major structural/environmental findings — **D28**: Sysmon's EID-3 telemetry is hard-filtered to ports 80/443 only, making any rule requiring a different port structurally unfireable (confirmed via direct Sysmon config read; flagged as a dedicated Codex/config task, not yet actioned); **D29**: a fully successful HTTPS round-trip from `cscript.exe` to `dns.google:443` produced zero Sysmon telemetry of any kind — root cause not fully isolated, `NET_DNS_SCRIPT_ENGINE_001` left INCONCLUSIVE, flagged as a genuine telemetry visibility gap for Section 2 of the research paper regardless of whether the mechanism is later pinned down. Also confirmed D22: Sysmon's NetworkConnect hook does not reliably observe loopback (127.0.0.1) traffic — use 8.8.8.8/8.8.4.4 for all network-layer simulations going forward, already applied correctly in Subphase 4.
- **2026-07-29 — Phase 7A Subphase 1 environmental & documentation findings (D1–D8).** Full detail in `docs/decisions_log.md` Entry 008. Summary: stale VMware shared-folder bookmark stalled the collector for >24h until cleared (pre-flight, resolved); `powershell.yaml`'s live 11-rule set diverges from documentation in 6 of 11 rules (2 absent, 2 renamed, 1 of the renamed also mechanistically different, 2 wholly undocumented); Windows Defender blocks 2 of 11 PowerShell rules' trigger commands pre-execution (AMSI bypass, credential-dumping signatures) — classified PARTIAL, not FAIL, consistent with Phase 4B precedent; DB timestamps are UTC while console `RULE_HIT` output and VM wall-clock are IST (UTC+5:30) — all future `--since`/`--until` bounds must be derived from DB-queried timestamps directly, not console output.
- **2026-07-14 — Subphase 4 corrections (event_fk join, DB_PATH import, explicit ordering).** *(Phase 5, retained for reference — see prior revision for full text.)*
- **2026-07-14 — Subphase 3 corrections (rule_id/LOLBIN_, EID-scoped first-event merge).** *(Phase 5, retained for reference — see prior revision for full text.)*
- **2026-07-12 — Phase 4B closure decision on Issue 4.** *(retained for reference — see prior revision for full text.)*

*(older decision entries trimmed for length — full history preserved in prior status.md revisions and permanently in `docs/decisions_log.md`)*

**⚠ Outstanding file-delivery debt:** `docs/decisions_log.md` has NOT yet been updated as an actual file with Entries 009–010 (covering D9 through D43). This status.md now reflects the correct summarized state, but the permanent decisions log file itself still needs the full entries written and delivered. This should happen in the same session as, or immediately after, this status.md update — see the companion decisions_log.md addendum being delivered alongside this file.

---

## Known Blockers / Open Items

**Phase 7A — active, in progress:**


- Subphase 4 (Parent-Child Chains) complete — see Recent Activity above. Next: Subphase 5 (API/OpenProcess).
- **D30 / Phase 4B Issue 4 — trending toward needing resolution.** `API_OPEN_PROCESS_SUSPICIOUS_ACCESS_001` firing unprompted from `wmiprvse.exe`/`vmtoolsd.exe` against `winlogon.exe`/`lsass.exe` has now recurred 3× (Subphase 3 pre-flight, Subphase 4 pre-flight — both idle/unprompted, both excluded from export per standing policy). Not yet risen to "must resolve now," but a 4th occurrence during **deliberate** Subphase 5 simulation specifically will meet the threshold to finally resolve Issue 4's disposition.
- **D28 (HIGH PRIORITY, Codex/config task, not yet actioned):** Sysmon's NetworkConnect (EID-3) filter only captures ports 80/443 — `NET_SUSPICIOUS_PORT_001` and `NET_SMB_LATERAL_001` are structurally unfireable until the Sysmon config's include filter is expanded. Deferred, not blocking Phase 7A/7B.
- **D29 (research-paper-relevant, not actioned):** genuine Sysmon telemetry visibility gap — a verified successful HTTPS round-trip from a script engine produced zero Sysmon events. `NET_DNS_SCRIPT_ENGINE_001` remains INCONCLUSIVE. Deferred, not blocking.
- **D43 (Codex fix candidate, not yet actioned):** `CHAIN_SCHEDULED_TASK_SCRIPT_001` cannot fire in practice on this Windows build despite a YAML update intended to fix it (schtasks.exe is never the true parent — always `svchost.exe -s Schedule`). Recommend deprecating or explicitly documenting as legacy-only coverage. Deferred, not blocking.
- **D46 (new, Subphase 3):** mshta.exe HTTP/HTTPS connections produce 0 EID-3 NetworkConnect events but CAN produce EID-22 (DNS). mshta appears to use WinINet or a browser-embedded HTTP stack invisible to Sysmon's EID-3 hook. All mshta-as-HTTP-initiator paths in SP3 were FAIL. Fix scope: SP4/SP5 — avoid using mshta for EID-3 simulation; HTA files only valid for EID-1 chains and EID-22.
- **D47 (new, Subphase 3):** nslookup.exe produces 0 EID-22 events. nslookup bypasses the Windows DNS client resolver (which is what Sysmon's EID-22 hook intercepts) and queries DNS servers directly via UDP/53. Not a rule defect. Fix scope: replace nslookup with PowerShell Resolve-DnsName or [System.Net.Dns]::GetHostEntry in any future DNS-rule paths.
- **D48 (new, Subphase 3):** EID-3 generation for port-80 connections is process-dependent on this VM. powershell, wscript, cscript hitting http://8.8.8.8:80 → 0 EID-3; odbcconf hitting the same target → 4 EID-3 hits. Root cause unclear (possible: WinHTTP vs raw WinSock stack difference; possible: refused TCP connection handling). Safe workaround: use https://8.8.4.4:443 as the target for all EID-3 rules where the process uses WinHTTP.
- **D50 (new, Subphase 4):** CHAIN_REGSVR32_CHILD_001 — regsvr32 + compiled C# COM DLL ([ComRegisterFunction] → Process.Start) produced 0 EID-1 events (all 3 paths). DLL compiled; regsvr32 ran silently. Root cause: Defender behaviorally terminates the child process before Sysmon EID-1. Confirmed: same DLL via regasm → PASS, proving C# code is correct. Fix scope: regsvr32 simulation needs a native DLL (not .NET); deferred.
- **D51 (new, Subphase 4):** CHAIN_LOLBIN_CHILD_001 Path B — cmstp /s /ni + RunPreSetupCommandsSection INF → 0 EID-1. Root cause: cmstp likely uses ShellExecute (parent = explorer/svchost, not cmstp) OR Defender blocked the chain. Rule confirmed via mshta (Path A) and regasm (Path C). Deferred.
- **D52 (new, Subphase 5):** Sysmon EID-10 not generated for denied `OpenProcess` (WinError 5). Confirmed across Rule 1 Path C (csrss, 0x1fffff), Rule 2 Path C (services.exe, 0x1440), Rule 4 Path C (csrss, 0x0028). Sysmon's ObRegisterCallbacks EID-10 hook only fires when the handle IS granted — access-denied attempts are silently dropped. PPL-protected processes (csrss) and processes with restrictive DACLs cannot be covered via ctypes simulation. Rules 1/2/4 are PARTIAL (2/3 paths confirmed). Not a rule defect.
- **D53 (new, Subphase 5):** `API_AV_PROCESS_ACCESS_001` structurally unfireable on this VM. Handles to fake msmpeng.exe/mpcmdrun.exe (notepad copies in Temp) were acquired successfully but 0 rule hits. Sysmon ProcessAccess filter does NOT monitor access to processes named msmpeng.exe/mpcmdrun.exe in C:\Windows\Temp — only covers its configured inclusion list (lsass, winlogon, csrss, notepad, etc.). Real MsMpEng.exe is PPL — all access denied. Rule is unfireable. Fix scope: either expand Sysmon config to include AV process names as targets, or accept this rule as untestable in this environment.
- **D54 (new, Subphase 5):** Sysmon EID-7 not generated for DLL loads by python subprocess (`API_DLL_LOAD_SUSPICIOUS_PATH_001` all 3 paths — 0 hits despite subprocess launches completing). Sysmon ImageLoad filter on this VM does not capture python.exe loading DLLs. Additionally, Defender quarantined `C:\Users\Public\ss_api.dll` during simulation (missing at cleanup — WinError 2). Paths A/C (Temp, ProgramData) confirmed not quarantined, but still 0 EID-7. Rule is unfireable via python subprocess approach.
- **D55 (new, Subphase 5):** Sysmon EID-7 not generated for rundll32/regsvr32 loading unsigned .NET DLL (`API_LOLBIN_DLL_UNSIGNED_001` all 3 paths — 0 hits). rundll32 with Temp DLL timed out at 20s (DLL was loaded — process hung — but no EID-7 generated). regsvr32 completed quickly, 0 hits. rundll32 with Public DLL completed quickly (DLL already quarantined by Defender). Sysmon ImageLoad filter does not capture these LOLBin DLL loads on this VM. Rule is unfireable without Sysmon config change.
- **D49 (new, Subphase 3):** EID-3 destination_hostname field is always NULL on this VM. Any rule with a `destination_hostname not_contains "X"` exclusion is structurally ineffective — it will always evaluate as "not excluded" because NULL doesn't contain any string. NET_POWERSHELL_HTTP_001 FP suppression FAIL is the direct consequence. Fix scope: Codex should change hostname-based exclusions to use `command_line not_contains "X"` for rules where the target hostname appears in the initiating process's command line.
- **Documentation debt carried forward:** `powershell.yaml` discrepancies (D1–D6) plus the now much larger set of `lolbins.yaml`/`network.yaml`/`parent_child.yaml` discrepancies (D9–D39) need resolution before Phase 10B paper drafting — implement/deprecate/sync IDs and document undocumented live rules. Not blocking Phase 7A continuation — flagged for a future Codex documentation-sync pass.
- **Office not installed on sandbox VM** — confirmed by Ayush. Has now caused SKIPs in Subphase 4 (`CHAIN_OFFICE_POWERSHELL_001`, `CHAIN_OFFICE_CMD_001`, `CHAIN_OFFICE_WSCRIPT_001` — 3 of 10 live parent_child rules) exactly as anticipated.
- **Atomic Red Team T1055 presence unconfirmed** — will verify at Subphase 6 pre-flight per task.md's own instruction; do not assume present.
- **ProcDump presence unconfirmed** — Ayush does not recall installing it. Decision deferred to Subphase 5: either install genuine Sysinternals ProcDump, or use the documented .NET P/Invoke `OpenProcess` fallback in task.md. Not blocking.
- **Timestamp discipline going forward:** all subphase `--since`/`--until` export bounds must be derived by querying the DB directly for the actual UTC timestamps of the first/last simulation in that subphase's window — not from console `RULE_HIT` output or VM wall-clock time, which are both IST and ~5.5 hours ahead of what's stored. Confirmed working correctly again in Subphase 4.
- **New working convention from Subphase 4 (applies to all future subphases):** bookmark-timestamp-newer-than-log-timestamp does NOT by itself indicate a stall — cross-check the `events` table directly before assuming the pipeline is broken (see handover 3.7 addendum / 3.9).
- **New working convention from Subphase 4 (applies to all future subphases):** when a rule doesn't fire, diagnose telemetry in order (pipeline alive → parent EID-1 → child EID-1 wide window → raw Sysmon event log directly) before concluding "rule problem" — see handover 3.9 in full.
- **NEW — Subphase 1-4 CSV staleness question (not yet decided):** Category A/B fixes (post-dating those subphases' exports) touched `CHAIN_SCHEDULED_TASK_SCRIPT_001` (Subphase 4 scope) and added `NET_SCRIPT_ENGINE_OUTBOUND_001` (Subphase 3 scope, rule didn't exist at export time). Whether to re-simulate Subphases 1-4 as well once the consolidated fix session closes, or accept existing CSVs as-is with this documented, is unresolved. See handover.md Section 21.5.
- **NEW — E5 (Sysmon `kernel32.dll` CreateRemoteThread exclusion), config fix deferred:** confirmed genuine detection blind spot (any real `LoadLibrary`-based reflective injection is currently invisible to this Sysmon deployment, independent of rule quality). Fix path known (remove/narrow the `StartModule` exclusion in `sysmonconfig-export.xml`) but explicitly deferred to a dedicated post-Phase-7A task, bundled with D28. Not blocking current work — simulation-side workaround (ntdll.dll/RtlExitUserThread) already in use. See handover.md Section 21.7 and `phase7a_subphase5-7_issues_log.md` entry E5.

- **E1 (CLOSED) / E2 (CLOSED) — API_OPEN_PROCESS_VM_WRITE_001
  consolidated fix session COMPLETE.** Both false-positive mechanisms
  fixed and verified. Test suite at 680 passed, 51 rules. All changes
  uncommitted on top of cda95c3. Subphase 7 next.
- **Subphase 3 git-status flag, not yet independently confirmed:**
  four files beyond E1's scope showed as modified/untracked at E1's
  closing verification. Assessed as pre-existing per project history,
  not content-diff-verified per the Category C precedent (Entry 011).
  Low risk, but flag before treating E1 as formally, fully closed.

**Phase 6B — carried forward (not blocking current work):**
- **Circular import: `dashboard.routers.pages` ↔ `dashboard.app`** — pre-existing defect, out of scope for Phase 6B. Fix should be done at the start of Phase 7A before any new dashboard routes are added (no dashboard routes have been touched in 7A through Subphase 4). Full suite unaffected (586 passed, 0 failed). See decisions_log.md Entry 007.
- **Feature-influence note for Phase 7B — RESOLVED:** `open_process_suspicious_access` and `hour_of_day` were confirmed anti-discriminative/low-signal and dropped from the RF feature set. See `progress_log_phase7b.md` SP1 entry for full details.

**Deferred to later phases (unchanged):**
- Phase 8A: Alert correlation, deduplication, suspected_families population
- Phase 9: Packaging, Windows Service, tray app

---

*Instructions for builder agents reading this file: only do the work listed under "Current Phase." Read `phase7a_task.md` for the full Phase 7A specification, and `phase7a_handover.md` for full working conventions (3.1–3.9) and the complete D1–D43 discrepancy log. Current target is Phase 7A Subphase 5 — API/OpenProcess (4 rules). Whenever you report progress back, also output the fully updated version of this entire file — status fields adjusted, a new line added to Recent Activity (trim oldest if over ~8 entries) — ready to paste as-is into the next message. If you finish a sub-task, get blocked, or hit an ambiguity, flag it clearly in that update.*
