# ShadowSensor — Live Status

*Single shared status file. Paste the current version of this file into the start of any Codex or dedicated Claude session prompt so that surface knows exactly where the project stands, with no need to recall prior conversations. Maintained by the ShadowSensor Prompt Agent — updated every time Ayush brings a progress report back.*

**Last Updated:** 2026-07-28 — Phase 6B FULLY CLOSED. Full regression 586 passed, 0 failed. Live VM end-to-end verification CONFIRMED by Ayush (full chain Sysmon → scoring → ML Insights, all 8 other dashboard pages clean). Git commit `dee6ef3` on origin/main. **Phase 6B COMPLETE ✅**.

---

## Current Phase
**Phase 6B — Isolation Forest Training & Real-Time Scoring Integration — COMPLETE ✅**

**Phase 6A — Benign Baseline Collection: COMPLETE ✅**
**Phase 5 — Feature Engineering Pipeline: COMPLETE ✅**

---

## Full Phase Tracker

*Condensed view — full detail lives in `ShadowSensor_Master_Implementation_Plan__2_.md`. Timeline currently tracking ~52 days (~7.4 weeks) after AV-style dashboard & tray icon feature additions.*

| Phase | Sub-step | Surface | Status |
|---|---|---|---|
| 0 | 0A — Build & validate Validation Lab | Dedicated Claude session | **Complete** ✅ |
| 0 | 0B — Repo + deps + standards | Codex | **Complete** ✅ |
| 1 | Event Collector & Normalizer | Codex | **Complete** ✅ |
| 2 | 2A — Rule engine core + starter rules | Codex | **Complete** ✅ |
| 2 | 2B — Validate starter rules in sandbox | Cursor (Claude Sonnet 4.6) | **Complete** ✅ |
| 3 | Storage + Dashboard MVP | Codex 5.3 / Auto-selection | **Complete** ✅ |
| Hold | Kill Chain Vis — Subtask A | Codex / Cursor | Complete |
| Hold | Kill Chain Vis — Subtask B | Codex / Cursor | Complete |
| Hold | Kill Chain Vis — Subtask C | Codex / Cursor | Complete |
| 4 | 4A — Full rule expansion | Codex | **Complete** ✅ |
| 4 | 4B — Validate all rules in sandbox *(+ family-template enrichment)* | Dedicated Claude session | **Complete** ✅ |
| 5 | Feature Engineering pipeline | Codex | **Complete** ✅ |
| 6 | 6A — Benign baseline collection | Dedicated Claude session | **Complete** ✅ |
| 6 | 6B — Train Isolation Forest | Cursor (Claude Sonnet 4.6) | **Complete** ✅ |
| 7 | 7A — Generate labeled suspicious telemetry *(+ family-template enrichment)* | Dedicated Claude session | Not Started |
| 7 | 7B — Train Random Forest | Codex | Not Started |
| 8 | 8A — Alert correlation/severity engine *(+ `suspected_families` field)* | Codex | Not Started |
| 8 | 8B — Validate severity tiers | Dedicated Claude session | Not Started |
| 9 | 9A — Packaging + service wrapper *(+ tray companion app — 1 → 3 days)* | Codex | Not Started |
| 9 | 9B — Fresh-install validation (Win10/11) *(+ tray validation — 1 → 1.5 days)* | Dedicated Claude session | Not Started |
| 10 | 10A — Run evaluation, generate metrics | Codex | Not Started |
| 10 | 10B — Research synthesis & paper draft *(+ Section 3.5 technique-to-family table)* | Dedicated Claude session | Not Started |

---

## Recent Activity (most recent first)

- 2026-07-28 — **Phase 6B FULLY CLOSED.** Both closure gates resolved: (1) Live VM end-to-end verification CONFIRMED by Ayush — full chain Sysmon → Collector → Normalizer → Rule Engine → Storage → Scoring → `model_scores` → ML Insights dashboard, all 8 other dashboard pages confirmed with no regressions. (2) Git commit `dee6ef3da11fa80744d7150b381bed0ab10514e4` pushed to `origin/main` and verified — 58 files changed, 7985 insertions, 90 deletions (combined Phase 6A + 6B). Final regression: **586 passed, 0 failed**. Stray files `'2026-07-27`, `check_range.py`, `check_range2.py` deleted. `Git_Upload_Commands_Log.md` left untracked/local. Phase 7A/7B ready to begin on Ayush's explicit go-ahead.
- 2026-07-28 — **Phase 6B Subphase 4 complete.** ML Insights dashboard (`/dashboard/ml-insights`) wired to real `model_scores` data. New `dashboard/services/ml_insights_service.py` service layer provides: `get_isolation_forest_status()` (all-time count, min/max/mean/median, 10-bracket distribution) and `get_score_trend()` (hourly averages, last 24h). Template fully redesigned with stats row, CSS distribution bars (color-coded: normal/elevated/anomalous), and ApexCharts trend line chart. `/api/v1/ml-status` wired to return live `models_trained=True` when data present. Random Forest section remains placeholder ("Phase 7B"). 24 new tests added. Full regression: **586 passed, 0 failed**. **LIVE VM BROWSER VERIFICATION REQUIRED** — see VM_RUN_GUIDE.md Phase 6B Subphase 4 section for exact checks. Hard stop — awaiting Ayush's go-ahead for Subphase 5.
- 2026-07-28 — **Phase 6B Subphase 3 complete.** Real-time Isolation Forest scoring integrated into the live pipeline. New `ml/scoring/scorer.py` module with `EventScorer` class loads the persisted artifact once at startup and scores each Sysmon event per-event via `EventFeatureExtractor` (no window aggregation, per Entry 003). `handle_persist_and_score_event` function added to `run_pipeline.py` — calls `persist_pipeline_event` directly to obtain `event_db_id` for `model_scores.event_fk` linkage, then scores and writes to `model_scores`. All four steps (event conversion, feature extraction, model scoring, DB write) wrapped in independent try/except with `logger.error` — no silent swallowing (Phase 6A lesson enforced). Pipeline degrades gracefully when model artifact is absent (warning logged, scoring disabled, pipeline continues). Full regression suite: **562 passed, 0 failed** (28 new Subphase 3 tests added). VM live verification required — see VM_RUN_GUIDE.md Phase 6B Subphase 3 commands. Hard stop — awaiting go-ahead for Subphase 4.
- 2026-07-28 — **Phase 6B Subphase 2 corrected validation complete.** Original in-sample validation (scoring benign_baseline.csv's 621 aggregated rows against the model that trained on them) was invalid and has been replaced. Corrected validation: 722 real individual, unaggregated Sysmon events (EID 1: 200, EID 3: 97, EID 7: 200, EID 10: 200, EID 22: 25) pulled from live DB at `C:\ShadowSensor\data\shadowsensor.db`, each run through `EventFeatureExtractor` alone (no `ProcessWindowAggregator`), scored with the trained model. Distribution: min=0.0106, max=0.6427, mean=0.2032, median=0.0946, std=0.2018, **variance=0.0407** (vs benign-baseline in-sample 0.0268). Bimodal EID-driven structure: EID 7/10 cluster near 0 (sparse vectors), EID 1/3/22 cluster in 0.3–0.65 range. EID 3 uniform score (0.3412) explained: all 97 events are HTTPS port 443, identical feature vectors. No clustering near 1.0. **PROCEED CONDITION MET.** 1 new test added (`test_per_event_scoring_non_degenerate`). Full suite: **534 passed, 0 failed**. Hard stop — awaiting go-ahead for Subphase 3.
- 2026-07-28 — **Phase 6A complete. Phase 6B Subphase 0 started.** Benign baseline `data\features\benign_baseline.csv` confirmed: 621 rows, 31 columns (30 features + label=0). Two blockers found and fixed during 6A: (1) silent SQLite write-path failure (early-return gate + swallowed exceptions); (2) feature extraction time-scoping gap (--since/--until added). Full suite baseline confirmed at **514 passed, 0 failed**. Phase 6B Subphase 0 complete: `docs/decisions_log.md` created and seeded with two pre-decided Phase 6B design decisions. VM clock discrepancy caveat noted (affects `hour_of_day`/`is_off_hours` features; non-blocking for Isolation Forest training). `open_process_suspicious_access` at ~39% activation in benign baseline (WARP-cluster explained) flagged as caveat for Phase 6B feature-contribution check. Hard stop after Subphase 0 — awaiting go-ahead for Subphase 1.
- 2026-07-17 — **Phase 5 complete — Feature Engineering Pipeline fully implemented and end-to-end verified on host PC. 502 tests passing. Definitive closure report produced.** All 14 Phase 5 source/test/doc files verified present. FEATURE_REGISTRY confirmed at exactly 30 entries in correct order. All structural checks pass: corrected descriptions, aggregator uses `rule_id`/`LOLBIN_`, pipeline uses `event_fk` join + `ORDER BY timestamp ASC`, `DB_PATH` imported from `storage.database`. Full suite: 502 passed, 0 failed. Phase 5 isolated: 90 passed, 0 failed. Both smoke tests pass (missing-DB header-only CSV; labeled 31-column export). Live host DB at `C:\ShadowSensor\data\shadowsensor.db` currently empty (Sysmon pipeline not yet producing events on host). CSV header structure confirmed correct (31 columns: 30 features + label). VM_RUN_GUIDE.md not yet updated with Phase 5 commands — flagged for Phase 6 pre-work. Phase 6 awaiting Ayush's explicit go-ahead.
- 2026-07-14 — **Phase 5 Subphase 4 complete (pipeline orchestrator + dataset exporter + CLI).** Added `ml/features/pipeline.py` with `FeatureExtractionPipeline.run()` using raw `sqlite3` + `sqlite3.Row`, process-window grouping by `(image, pid)` with `null_pid` handling, event-id-to-window mapping, `rule_hits` assignment via `event_fk`, and explicit `ORDER BY timestamp ASC` for both events and rule_hits to preserve first-event semantics. Added `ml/features/exporter.py` with `export_to_csv()` (stable `FEATURE_NAMES` column order, optional `label`, header-only behavior on empty vectors, runtime directory creation) and `default_output_path()`. Added `scripts/run_feature_extraction.py` CLI with `--label`, `--output`, `--db`, importing default `DB_PATH` from `storage.database` (not hardcoded), plus graceful missing-DB warning path writing header-only CSV with exit code 0. Added `tests/test_phase5/test_pipeline.py` with 15 tests covering pipeline grouping, label behavior, `event_fk` joins/skips, null-pid handling, exporter behavior, and nonexistent DB behavior. Verification: `python_runtime/python.exe -m pytest tests/test_phase5/test_pipeline.py -v` → 15/15 passed; `python_runtime/python.exe -m pytest tests/ -v --tb=short -q` → 502 passed, 0 failed. Smoke tests: missing DB CSV header pass, default DB export pass, labeled export label-column pass. Hard stop — Phase 5 overall completion gate must be checked against corrected 502 total before declaring Phase 5 complete.
- 2026-07-14 — **Phase 5 Subphase 3 complete (process-window aggregator).** Added `ml/features/aggregator.py` with `ProcessWindowAggregator.aggregate(event_vectors, rule_hits)` using corrected ground truth: rule-hit prefix checks and uniqueness from `rule_id` (with `None` guarding), LOLBin prefix `LOLBIN_`, and first-event feature resolution via EID-scoped tuple inputs `(event_type_id, vector_dict)` instead of ambiguous "first non-default value" logic. Added `tests/test_phase5/test_aggregator.py` with 22 tests covering sum/OR/first-event merge paths, rule-hit prefixes, key-shape guarantees, and regression checks for `rule_name` misuse and non-default ambiguity. Appended schema corrections to `docs/phase5_schema_reference.md` and logged decisions. Verification: `python_runtime\python.exe -m pytest tests\test_phase5\test_aggregator.py -v` → 22/22 passed; `python_runtime\python.exe -m pytest tests\ -v --tb=short -q` → 487 passed, 0 failed. Hard stop — Subphase 4 not started.
- 2026-07-14 — **Phase 5 Subphase 2 complete (per-event feature extractor).** Added `ml/features/extractor.py` (`EventFeatureExtractor.extract()`, `shannon_entropy()`) consuming the confirmed schema from `docs/phase5_schema_reference.md`: flat `event_type_id`/`pid`/`image`/`timestamp`, nested fields from `raw_json` per EID; `PureWindowsPath` for Windows basenames; `granted_access` (not access_mask); EID-1 `is_signed` always 0 (schema gap). Added `tests/test_phase5/test_extractor.py` with 41 tests (EID-7 signed coverage replacing obsolete EID-1 signed assumptions; hour_of_day covered for both datetime and ISO string). Verification: `python_runtime\python.exe -m pytest tests\test_phase5\test_extractor.py -v` → 41/41 passed; `python_runtime\python.exe -m pytest tests\ -v --tb=short -q` → 465 passed, 0 failed. Hard stop — Subphase 3 not started.
- 2026-07-14 — **Phase 5 Subphase 1 complete (feature specification + registry tests).** Added `ml/features/feature_spec.py` with the authoritative 30-entry `FEATURE_REGISTRY`, `FEATURE_NAMES`, `default_feature_vector()`, and all four constant sets (`LOLBIN_NAMES`, `SUSPICIOUS_PARENT_IMAGES`, `SUSPICIOUS_CHAINS`, `SUSPICIOUS_PORTS`). Added `tests/test_phase5/test_feature_spec.py` with all 12 required Subphase 1 tests plus package init files. Verification passed in required order: `python_runtime\python.exe -m pytest tests\test_phase5\test_feature_spec.py -v` → 12/12 passed; `python_runtime\python.exe -m pytest tests\ -v --tb=short -q` → 424 passed, 0 failed.
- 2026-07-13 — **Codex Subphase 6 complete (full rule-engine regression + gate closure).** Verified rule-engine startup loads 49 rules (`Loaded 49 rules from rules\definitions`), full automated regression passes clean at 412/412 (0 failures), and a single consolidated targeted simulation session confirms all five fixed detection paths fire (`API_AV_PROCESS_ACCESS_001`, `NET_DNS_LONG_QUERY_001`, `CHAIN_SCHEDULED_TASK_SVCHOST_001`, `API_CREATE_REMOTE_THREAD_001`, `NET_DNS_SCRIPT_ENGINE_001`). Fresh synthetic benign-baseline sweep across expanded rule tests and false-positive regression corpus passed clean (78/78 selected negative-path checks, 0 failures), with no new false positives observed. Per-rule test-count gate remains consistent with baseline 399 + 13 fix-pass additions = 412 total. Phase 4B pre-Phase-5 blocker is now closed; Phase 5 is authorized to begin only on explicit go-ahead.
- 2026-07-13 — **Codex Subphase 5 closure decision applied (Issues 2 & 6 environmentally limited, no code fix).** The proposed enrichment-layer normalizer fix path was explicitly rejected because it introduces unacceptable false-positive risk (behavioral drift and PID reuse misattribution) that cannot be guaranteed FP-free under the rule-engine true-positive-only constraint. Confirmed root cause remains upstream Sysmon XML identity failure (`<unknown process>` and all-zero GUID-origin values) with normalizer passthrough behavior verified as correct. Issues 2 and 6 are now closed as environmentally limited and reserved for the research paper's Section 2 (Telemetry Design). Project position updated as ready for Subphase 6.
- 2026-07-13 — **Codex Subphase 4 diagnosis complete (read-only, no fix applied).** Confirmed in code that `normalizer/parser.py` performs direct Sysmon XML field mapping via `FIELD_MAPS` with no PID→Image/ProcessGuid lookup function, no process-table query, and no cache resolver path. EID 8 (`CreateRemoteThreadEvent`) target resolution is direct `TargetImage` passthrough; EID 22 (`DnsQueryEvent`) source image resolution is direct `Image` passthrough, while `ProcessGuid` is not modeled/mapped for EID 22 at all (`normalizer/field_maps.py` + `normalizer/models.py`), so the normalizer cannot preserve or expose GUID identity for DNS events. This confirms the shared mechanism is schema-level passthrough (not race/stale-cache lookup logic). Proposed fix documented for review only; hard stop maintained pending explicit approval before Subphase 5.
- 2026-07-13 — **Codex Subphase 3 correction applied (two-rule split for Issue 9).** Reverted `CHAIN_SCHEDULED_TASK_SCRIPT_001` parent-side logic to legacy scheduler parents only (`taskeng.exe`, `taskhostw.exe`, `schtasks.exe`) with no `parent_command_line` condition, and introduced new sibling rule `CHAIN_SCHEDULED_TASK_SVCHOST_001` for the scoped service-host path (`parent_image=svchost.exe` + `parent_command_line` contains `-s Schedule`) while keeping the child-side suspicious script/execution markers identical between both rules. Updated `tests/test_phase4a/test_parent_child_rules.py` to validate: svchost Schedule true-positive on the new rule, independent legacy-parent true-positives on the original rule without parent-command-line requirements, and svchost non-Schedule negative behavior for both rules. Required verification sequence completed; full regression passing with zero failures. Hard stop maintained; Subphase 4 not started.
- 2026-07-13 — **Codex fix pass (task.md) Subphase 3 complete.** `CHAIN_SCHEDULED_TASK_SCRIPT_001` coverage gap fix applied in `rules/definitions/parent_child.yaml` by extending parent-image matching to include `svchost.exe` and adding a paired `parent_command_line` condition that requires one of the scheduler-identifying markers (`taskeng.exe`, `taskhostw.exe`, `schtasks.exe`, `-s Schedule`). This preserves the three original parent match paths while adding the validated `svchost.exe -s Schedule` Task Scheduler service-host path (Issue 9) and preventing non-Schedule `svchost.exe` service launches from matching. Added targeted coverage in `tests/test_phase4a/test_parent_child_rules.py` for the confirmed Schedule-service true-positive path, explicit non-Schedule `svchost.exe` negative path, and regression protection for all three original parent executables. Verification passed in required order; full regression now 412/412 passing. Hard stop observed after Subphase 3.
- 2026-07-13 — **Codex fix pass (task.md) Subphase 2 complete.** `NET_DNS_LONG_QUERY_001` false-positive fix applied in `rules/definitions/network.yaml` by excluding `SearchApp.exe` from source-image matches (Issue 3) while preserving the existing 50+ character query threshold logic. Added targeted coverage in `tests/test_phase4a/test_network_rules.py` for the confirmed PowerShell true-positive (`aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa.example.com`), SearchApp negative path using the validated `footprintdns.com`-style long hostname pattern, and 49/50/51 boundary behavior to ensure threshold semantics and exclusion behavior both remain correct. Verification passed in required order; full regression now 407/407 passing. Hard stop observed after Subphase 2.
- 2026-07-13 — **Codex fix pass (task.md) Subphase 1 complete.** `API_AV_PROCESS_ACCESS_001` false-positive fix applied in `rules/definitions/api_memory.yaml` by adding a dedicated source-process exclusion for `csrss.exe`, `conhost.exe`, `svchost.exe`, `lsass.exe`, `winlogon.exe`, `wininit.exe` (Issue 1). Corresponding tests expanded in `tests/test_phase4a/test_api_memory_rules.py` to assert true-positive behavior for non-excluded sources and negative behavior for each excluded source. Subphase checks passed; full regression now 404/404 passing in current environment. Hard stop observed after Subphase 1.
- 2026-07-12 — **Phase 4B COMPLETE.** All 48 rules validated across Subphases 1–7. Final tally: PASS (including upgraded/re-validated) 39, PARTIAL/ENVIRONMENT-LIMITED 7, FAIL (confirmed bugs, routed to Codex) 2, SKIPPED 1. Deferred EID 8 (CreateRemoteThread) validation completed in Subphase 5. Subphase 7 benign baseline (clean session 22:49:29–23:07:35, 18m06s): 2 RULE_HITs — both pre-existing issues, not new FP classes (Issue 2 5th recurrence; Issue 3 upgraded from provisional to CONFIRMED and routed to Codex). `PS_DOWNLOAD_CRADLE_001`/`PS_HIDDEN_WINDOW_001` deferred FP tuning checks both closed clean (no FP observed). Issue 4 left explicitly open/inconclusive, not routed. 5 issues routed to Codex total (Issues 1, 2, 3, 6, 9); 2 documentation-only environment-limitation findings (Issues 7, 8) reserved for the research paper. Family enrichment notes logged for all 48 rules per the malware family context enrichment feature. Phase 5 (Feature Engineering) is next, pending a Codex fix pass for the 5 routed issues and a full 399-test regression re-run.
- 2026-07-07 — Phase 4A complete. Rule count expanded from 15 to 48 rules across 5 YAML files (66 new tests added, 399 total tests passing). New tactics now covered beyond original 15-rule set: Credential Access, Lateral Movement, Persistence. Phase 4B (sandbox validation of all rules) is next.
- 2026-07-06 — Kill Chain Visualisation holding phase closed. All three subtasks complete and VM-verified. 10th dashboard page (/dashboard/killchain) delivering ATT&CK tactic-stage correlation view with real-time HTMX polling, stage expansion panels, and Alert Feed integration. 333/333 tests passing. Phase 4A is next.
- 2026-07-03 — Kill Chain Visualisation Subtask C complete. Created killchain_stage_detail.html (inline expansion panel, rule breakdown table, View in Alert Feed links, collapse button). Added pulsing border animation for fired cards. 333 tests passing total. Host-side and VM verification complete. Kill Chain holding phase CLOSED. Next: Phase 4A full rule expansion.
- 2026-07-02 — Kill Chain Visualisation Subtask B complete. VM 500 root cause identified as mixed offset-naive/offset-aware datetime comparisons in `get_kill_chain_status()` (`TypeError: can't compare offset-naive and offset-aware datetimes`). Fixed by normalizing datetimes to UTC-aware in killchain service (`format_relative_time`, `get_kill_chain_status`, `get_tactic_rule_detail`). Final verification: 320 passed, 0 failed; VM endpoint checks all expected (`killchain page/overview/stage 200`, invalid tactic `404`, regressions `200`); browser validation passed with no internal server errors.
- 2026-07-02 — Kill Chain Visualisation Subtask B implementation complete. Created killchain.html (full page with HTMX polling, time range integration, 12 stage-detail expansion containers outside polling div), killchain_overview.html partial (12 tactic cards in ATT&CK kill chain order, coverage summary, last-updated indicator), Kill Chain nav item added to base.html, CSS section appended to main.css. Host-side verification complete: 320 tests passing and endpoint checks clean.
- 2026-07-02 — Kill Chain Visualisation Subtask A complete. Created killchain_service.py (load_rule_tactic_map, get_kill_chain_status, get_tactic_rule_detail, format_relative_time), killchain.py router (3 endpoints: /dashboard/killchain, /partials/killchain-overview, /partials/killchain-stage/{tactic_id}), test suite. Route spot-check: all 3 endpoints 200, INVALID_TACTIC returns 404, all 9 existing pages unaffected. 296 tests passing total. Rule map loaded: 15 rules, 4 unique tactics.
- 2026-07-02 — Phase 3 fully complete and VM-verified. All 9 dashboard pages confirmed loading in browser. Pipeline and dashboard both start cleanly. SQLite DB at C:\ShadowSensor\data\shadowsensor.db (local NTFS — VMware shared folder incompatibility resolved). 249/249 tests passing. Phase 3 closed.
- 2026-07-02 — SQLite local path fix applied. VMware shared folder filesystem incompatible with SQLite file locking — all SQLite operations including PRAGMA table_info() failed with disk I/O error. DB_DIR moved to C:\ShadowSensor\data\ (local NTFS). Configurable via SHADOWSENSOR_DB_DIR env var. Only storage/database.py modified. Fix confirmed by VM browser test.

---

## Decisions & Notes Log

- **2026-07-14 — Subphase 4 corrections (event_fk join, DB_PATH import, explicit ordering).** task.md's Subphase 4 spec assumed timestamp-window matching for rule_hits and a hardcoded default DB path; both were replaced with a direct event_fk join and an imported DB_PATH from storage.database, confirmed via pre-flight investigation. Explicit ORDER BY timestamp ASC added to both queries since ProcessWindowAggregator's first-event resolution depends on chronological order. Ground truth recorded in docs/phase5_schema_reference.md. Pipeline test count is 15 as task.md stated, but the running total is 502 (487 + 15), not task.md's assumed 500, due to Subphase 3's one extra test.
- **2026-07-14 — Subphase 3 corrections (rule_id/LOLBIN_, EID-scoped first-event merge).** task.md's Subphase 3 spec had three issues: (1) it assumed rule_name and a 'LOL_' prefix for rule-hit matching — actual schema uses rule_id and 'LOLBIN_', confirmed against live rule definitions; (2) its 'first non-default value' merge rule for first-event features was ambiguous, since several features' defaults (e.g. is_off_hours=0) are also legitimate computed values — corrected to resolve first-event features by matching each vector's source EID instead; (3) event_vectors is now passed as (event_type_id, vector) tuples rather than bare dicts to support this. Ground truth recorded in docs/phase5_schema_reference.md. Aggregator test count is 22 (not task.md's stated 20) — running total after Subphase 3 is 487, not 485.
- **2026-07-12 — Phase 4B closure decision on Issue 4.** `API_OPEN_PROCESS_SUSPICIOUS_ACCESS_001`'s `wmiprvse.exe`/`svchost.exe` → `winlogon.exe`/`lsass.exe` pattern was left explicitly unresolved rather than forced to a verdict at Phase 4B close — insufficient evidence across sessions to classify as either a stable FP surface or a fully benign-explained pattern. Not routed to Codex; flagged for revisiting if it recurs.
- **2026-07-01 — Sub-task D delivery.** Four advanced pages: Query Console (`/dashboard/search`), ML Insights placeholder (`/dashboard/ml-insights`), Rules Library (`/dashboard/rules`), Settings (`/dashboard/settings`). Ack/Resolve buttons now update status badge in-place via `GET /dashboard/partials/alert-status/{id}`. Health partial at `/dashboard/partials/health`. 22 E2E smoke tests. Full suite 249/249.
- **2026-06-28 — Sub-task C dashboard delivery.** Full CSS variable system (dark/light), base.html with 8-nav sidebar + time range picker, 5 core pages with HTMX partials. Charts on Home via JS fetch() every 5s; Alert Feed HTMX 3s polling with KQL pause condition; Home recent-alerts HTMX 5s. Ack/Resolve buttons use hx-patch to frozen API with hx-swap="none" + tbody reload (JSON API cannot return HTML rows).
- **2026-06-26 — Phase 3 architecture locked.** Storage: SQLite at `data/shadowsensor.db`, SQLAlchemy 2.0 sync sessions, WAL mode. Backend: FastAPI + uvicorn port 8080. Templates: Jinja2 + HTMX 1.9.12 (HTML table refreshes). Charts: ApexCharts via CDN (JS fetch, not HTMX). KQL: lark LALR(1) grammar. Theme: CSS custom properties, dark default, localStorage persistence. CDN: HTMX, ApexCharts, flatpickr.
- **2026-06-26 — Phase 3 alert generation: Option A (1:1 rule_hit → alert).** Each rule hit creates one alert row immediately. No deduplication or correlation in Phase 3. `suspected_families` column present but always NULL until Phase 8A. Phase 8A will replace AlertManager stub with full correlation engine.
- **2026-06-26 — Phase 3 pipeline modification policy.** Only `scripts/run_pipeline.py` may be modified. Change is purely additive (new imports, new calls after existing log write). collector/, normalizer/, rules/ are frozen. Existing log output (logs/rule_hits.log) must continue unchanged.
- **2026-06-23 — OpenProcess redesign: target-centric, not cross-basename.** Cross-basename OpenProcess with broad access masks is routine Windows behavior. New rule fires only when target ends with lsass.exe, winlogon.exe, or csrss.exe. System callers (MsMpEng, csrss, lsass, winlogon, wininit) excluded via `not_ends_with_any`.
- **2026-06-22 — Event ID 8 deferred to Phase 4B.** CreateRemoteThread requires actual process injection. Will be validated in Phase 4B with Atomic Red Team T1055 simulation.
- Event ID 22 (DnsQuery) has a known limitation: browsers use DNS-over-HTTPS bypassing Sysmon's ETW hook. Expected behavior.
- Hypervisor = VMware Workstation Pro (free for personal use). Guest OS = Windows 10 Pro.
- ELK stack runs on the host machine directly, not a separate VM (lab confirmation bench only — not in shippable agent).
- Sysmon config file: C:\sysmon\sysmonconfig-export.xml (locked after Phase 0A testing).
- ChatGPT's role is complete (produced the phase-by-phase plan); not expected to receive further updates.

---

## Known Blockers / Open Items

**Phase 3 — resolved:**
- SQLite WAL + all pragma operations incompatible with VMware shared folder → resolved by moving DB to local NTFS path
- DetachedInstanceError on Alert Details page → resolved in pages.py (to_dict() called inside session block)
- Alert Feed table overflow → resolved with table-layout:fixed and colgroup widths

**Phase 4A — resolved:**
- Full rule expansion completed. Rule set now includes 48 rules across 5 YAML files and passes the full test suite.

**Phase 4B — COMPLETE, all findings resolved or explicitly routed:**
- All 48 rules validated across Subphases 1–7 (see `docs/phase4b_validation_log.md` for full detail).
- Event ID 8 (CreateRemoteThread) validation: complete (Subphase 5).
- Exclusion work for PS_DOWNLOAD_CRADLE_001 and PS_HIDDEN_WINDOW_001: deferred FP tuning check complete, both resolved clean — no FP observed during the Subphase 7 benign baseline, no exclusion needed.
- 5 confirmed rule/normalizer bugs were routed to Codex for fix pass before Phase 5 begins (Issues 1, 2, 3, 6, 9 — exact fix descriptions in the validation log); Issues 1 and 3 are fixed/verified, Issue 9 is fixed via the Subphase 3 two-rule correction (`CHAIN_SCHEDULED_TASK_SCRIPT_001` + `CHAIN_SCHEDULED_TASK_SVCHOST_001`), and Issues 2 and 6 are now closed as environmentally limited based on confirmed Sysmon-upstream identity-resolution failure.
- 2 documentation-only environment-limitation findings (Issues 7, 8) reserved for the research paper — not Codex-actionable.
- Issue 4 (`API_OPEN_PROCESS_SUSPICIOUS_ACCESS_001` FP pattern) left explicitly open/inconclusive — not routed, not closed, monitor if it recurs.

**Next up — pre-Phase-5 gate:**
- Gate status: **SATISFIED**. Full Codex fix pass (Subphases 1-6) is complete with final 49-rule load confirmation and 412/412 regression pass.
- Issues 2 and 6 remain correctly classified as environmentally limited (not fixable at rule/normalizer layer without violating the true-positive-only constraint) and are reserved for the research paper's Section 2 (Telemetry Design).
- Phase 5 (Feature Engineering Pipeline) may begin pending Ayush's explicit go-ahead. Do not start Phase 5 work until that instruction is given.

**Phase 6B — carried forward (not blocking current work):**
- **Circular import: `dashboard.routers.pages` ↔ `dashboard.app`** — `tests/test_phase3/test_e2e_smoke.py` fails with `ImportError` when run in isolation but passes in the full suite due to import-order side effects. Pre-existing defect, surfaced in Phase 6B Subphase 3 when new test files shifted import ordering. **Out of scope to fix in Phase 6B.** Fix should be done at the start of Phase 7A before any new dashboard routes are added. Full suite unaffected (586 passed, 0 failed). See decisions_log.md Entry 007.
- **Feature-influence note for Phase 7B:** `open_process_suspicious_access` (~39% benign activation, WARP-cluster explained) and `hour_of_day`/`is_off_hours` (VM clock discrepancy) were flagged as disproportionately influential during Isolation Forest training (Subphase 2 feature-contribution check). Not acted on in Phase 6B — carried to Phase 7B for awareness before Random Forest training against labeled suspicious data.

**Deferred to later phases (unchanged):**
- Phase 8A: Alert correlation, deduplication, suspected_families population
- Phase 9: Packaging, Windows Service, tray app

---

*Instructions for builder agents reading this file: only do the work listed under "Current Phase." Read task.md for the full Phase 3 specification. Current target is Phase 4A — Full Rule Expansion (~40–50 rules). Whenever you report progress back, also output the fully updated version of this entire file — status fields adjusted, a new line added to Recent Activity (trim oldest if over ~8 entries) — ready to paste as-is into the next message. If you finish a sub-task, get blocked, or hit an ambiguity, flag it clearly in that update.*
